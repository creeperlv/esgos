# esGOS

EMCAScript Glued Operating System

# Setup build/development

Obtain latest `daktape` and put them in `src` folder.

# Supported Devices

Currently M5PaperS3 is the only device tested.

But it should work on all devices from M5Stack.